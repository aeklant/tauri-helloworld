This project was created with the tauri-cli. It has not frontend framework (just html/js) to minimize variables (for learning).

i.e. it required the tauri-cli to be installed (cargo install tauri-cli), and then use the cargo tauri init command
